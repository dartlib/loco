import 'package:flutter/material.dart';

class AppLayout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // A layout in this case is within a MaterialApp already.
    // For this todo app I only need one layout
    // and probably only one slot
    return Container();
  }
}
